import { colors } from '@/constants/theme';

export default {
  light: {
    text: colors.ink,
    background: colors.plate,
    tint: colors.sauce,
    tabIconDefault: colors.muted,
    tabIconSelected: colors.sauce,
  },
  dark: {
    text: colors.cream,
    background: colors.board,
    tint: colors.crust,
    tabIconDefault: colors.muted,
    tabIconSelected: colors.crust,
  },
};
